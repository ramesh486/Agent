# Overview
This is a Terraform configuration that creates the Visual Builder service on Oracle Cloud Infrastructure.

This is a Terraform configuration that creates the Visual Builder service on Oracle Cloud Infrastructure.

The Terraform code is used to create a Resource Manager stack, that creates the required resources and configures the application on the created resources.

# pre request

1. The Visual Builder instance created only in the root level
2.  idcs Token (when deployment of this code freshly user should create idcs token)