# This resource provides the Certificate resource in Oracle Cloud Infrastructure API Gateway service.
 
 # prerequisties:

 1. The certificate in PEM format (After End of every line use \n)
 2. The private key in PEM format (After End of every line use \n)
