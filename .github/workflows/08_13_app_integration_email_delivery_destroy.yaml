
# On successful pull request merge, `terraform destroy` will be executed.
# Update PROVIDER_NAME, BRANCH_NAME, STACK_NAME, WORKING_DIRECTORY, REPO_URL, TF_VERSION

name: 'Destroy App Integration Email Delivery'
on:
  pull_request:
    types: [closed]
    branches:
      - 'dev8'
    paths:
      - '.github/workflows/08_13_app_integration_email_delivery_destroy.yaml'

# ---------------------------------------------
# Permissions
# ---------------------------------------------

permissions:
  contents: write

jobs:
  
  stack08_app_integration_email_delivery_destroy:
    uses: ./.github/workflows/action_runner_destroy.yaml

    # ---------------------------------------------
    # Source Provider Details
    # ---------------------------------------------

    with:

      PROVIDER_NAME: "oci_github"
      BRANCH_NAME: "feature_stack08_developer_services_destroy"
      STACK_NAME: "stack08_app_integration_email_delivery"
      WORKING_DIRECTORY: "CICD/stack08_developer_services/13.app_integration_email_delivery"
      REPO_URL: "https://github.com/ebizon-cloud/oci_automation.git"
      TF_VERSION: "1.2.x"

    # ---------------------------------------------
    # Secrets
    # ---------------------------------------------

    secrets:
      USER_OCID: ${{secrets.USER_OCID}}
      FINGERPRINT: ${{secrets.FINGERPRINT}}
      COMPARTMENT_OCID: ${{secrets.COMPARTMENT_OCID}}
      REGION: ${{secrets.REGION}}
      TENANCY_OCID: ${{secrets.TENANCY_OCID}}
      PRIVATE_KEY_FILE: ${{secrets.PRIVATE_KEY_FILE}}
