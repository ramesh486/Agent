# Service Mesh

Oracle Cloud Infrastructure Service Mesh allows to add a set of capabilities that enables microservices within a cloud native application to communicate with each other in a centrally managed and secure manner. Adding a service mesh is done by deploying a proxy alongside each microservice, which receives configuration information from a managed control plane. 

## Prerequisite:

1. Create a Certificate Authority OCID