# An entity is a resource in Logging Analytics which is used to reference the real asset on-premises host or virtual host. 
## Pre request
### Create a object storage bucket before deploying log analytics entity code

