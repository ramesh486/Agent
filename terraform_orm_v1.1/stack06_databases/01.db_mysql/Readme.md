# Pre-request
# Policy are required
# MySQL is a popular open-source relational database management system (RDBMS) that allows to store, manage, and retrieve data efficiently. While MySQL can be installed and configured in an Oracle Cloud Infrastructure (OCI) environment.