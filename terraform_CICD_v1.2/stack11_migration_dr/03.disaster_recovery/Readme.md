# DR Protection Group

A DR Protection Group groups together all the components of a full stack application so that all the components can be recovered together to restore the full stack application.

## Prerequisite:

* Create a OCID of the member (AUTONOMOUS_DATABASE, COMPUTE_INSTANCE, DATABASE, VOLUME_GROUP)
* Create an object storage bucket