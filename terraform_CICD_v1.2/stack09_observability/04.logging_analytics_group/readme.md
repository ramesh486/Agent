# Creates a new log group in the specified compartment with the input display name.
## Log Groups are located in a compartment to provide user access control to logs stored in that log group.
### pre request
#### create a object storage bucket before deploying loganalytics group code.