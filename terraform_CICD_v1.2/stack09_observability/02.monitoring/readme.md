# Monitoring resource provides the Alarm in Oracle Cloud Infrastructure Monitoring service.This call is subject to a Monitoring limit that applies to the total number of requests across all alarm operations.
# The Monitoring service uses metrics to monitor resources and alarms to notify when these metrics meet alarm-specified triggers. Metrics are emitted to the Monitoring service as raw data points,or timestamp-value pairs, along with dimensions and metadata.
# Pre request
 # a. Create a bucket in object storage service before creating alarms.
 # b. Provide a valid Email-id for Notifications.
 # c. Notification mail will be sent. Click confirm subscription to activate Topic subscription.